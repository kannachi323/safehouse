import TenantListingContainer from "@/containers/user-page/TenantListingContainer"

export default function User() {
    //should only allow to get here if user is signed in
    return (
      <TenantListingContainer />
      
    )
}